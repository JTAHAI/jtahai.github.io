# justintahai.github.io

This is my GitHub Pages site: systems notes, checklists, and writeups.

## Local preview (optional)
If you have Ruby + Bundler:

```bash
bundle install
bundle exec jekyll serve
```

Otherwise, edit the markdown files and push — GitHub Pages will build it.
